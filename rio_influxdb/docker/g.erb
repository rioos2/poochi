#!/bin/bash

sudo docker build -t rio-influxdb -f Dockerfile .

sudo docker tag rio-influxdb <%= @basic[:registry_url]+"/rioosinfluxdb:"+@package[:version] %>

sudo docker push <%= @basic[:registry_url]+"/rioosinfluxdb:"+@package[:version] %>
