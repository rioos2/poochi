#!/bin/bash

sudo docker build -t rio-fluentbit -f Dockerfile .

sudo docker tag rio-fluentbit <%= @basic[:registry_url]+"/rioosfluentbit:"+@package[:version] %>

sudo docker push <%= @basic[:registry_url]+"/rioosfluentbit:"+@package[:version] %>
