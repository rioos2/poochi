#!/bin/bash

mkdir -p <%= @basic[:libhome] %>/controller

mv <%= @basic[:home]+"/"+@package[:package] %>/rioos-scheduler.service /etc/systemd/system/

<!-- mkdir -p <%= @basic[:libhome] %>/context
cd <%= @basic[:libhome] %>/context

wget -O init.sh <%= @package[:tar] %>
cp <%= @basic[:home]+"/"+@package[:package] %>/VERSION .
cp <%= @basic[:home]+"/"+@package[:package]+"/bin" %>/rioos-gulpd gulpd -->

systemctl enable <%= @package[:systemd_service] %>
systemctl start <%= @package[:systemd_service] %>

systemctl enable rioos-scheduler.service
systemctl start rioos-scheduler.service
