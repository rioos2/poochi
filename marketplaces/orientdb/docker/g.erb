#!/bin/bash

sudo docker build -t rio-orientdb -f Dockerfile .

sudo docker tag rio-orientdb <%= @basic[:registry_url]+"/rioosorientdb:"+@package[:version] %>

sudo docker push <%= @basic[:registry_url]+"/rioosorientdb:"+@package[:version] %>
