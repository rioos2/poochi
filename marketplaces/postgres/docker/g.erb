#!/bin/bash

sudo docker build -t rio-postgres -f Dockerfile .

sudo docker tag rio-postgres <%= @basic[:registry_url]+"/rioospostgres:"+@package[:version] %>

sudo docker push <%= @basic[:registry_url]+"/rioospostgres:"+@package[:version] %>
