#!/bin/bash

sudo docker build -t rio-powerdns -f Dockerfile .

sudo docker tag rio-powerdns <%= @basic[:registry_url]+"/rioospowerdns:"+@package[:version] %>

sudo docker push <%= @basic[:registry_url]+"/rioospowerdns:"+@package[:version] %>
