#!/bin/bash

sudo docker build -t rio-couchdb -f Dockerfile .

sudo docker tag rio-couchdb <%= @basic[:registry_url]+"/riooscouchdb:"+@package[:version] %>

sudo docker push <%= @basic[:registry_url]+"/riooscouchdb:"+@package[:version] %>
