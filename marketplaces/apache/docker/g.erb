#!/bin/bash

sudo docker build -t rio-apache -f Dockerfile .

sudo docker tag rio-apache <%= @basic[:registry_url]+"/rioosapache:"+@package[:version] %>

sudo docker push <%= @basic[:registry_url]+"/rioosapache:"+@package[:version] %>
