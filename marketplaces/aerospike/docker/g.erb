#!/bin/bash

sudo docker build -t rio-aerospike:<%= @package[:version] %> -f Dockerfile .

sudo docker tag rio-aerospike <%= @basic[:registry_url]+"/rioosaerospike:"+@package[:version] %>

sudo docker push <%= @basic[:registry_url]+"/rioosaerospike:"+@package[:version] %>
