#!/bin/bash

sudo docker build -t rio-redis -f Dockerfile .

sudo docker tag rio-redis <%= @basic[:registry_url]+"/rioosredis:"+@package[:version] %>

sudo docker push <%= @basic[:registry_url]+"/rioosredis:"+@package[:version] %>
