#!/bin/bash

sudo docker build -t rio-vnc -f Dockerfile .

sudo docker tag rio-vnc <%= @basic[:registry_url]+"/rioosvnc:"+@basic[:version] %>

sudo docker push <%= @basic[:registry_url]+"/rioosvnc:"+@basic[:version] %>
